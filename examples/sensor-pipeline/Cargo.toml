[package]
name = "merkql-sensor-pipeline"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
merkql = { path = "../.." }
tempfile = "3"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
